# 🚀 HFT Bot: Гайд по Быстрому Развертыванию (Deployment Guide)

Этот гайд поможет за 5 минут поднять новый сервер в Токио и запустить на нем бота, если старый IP забанили.

---

## 1. Выбор и Покупка Сервера
*   **Провайдер:** Любой (Aeza, Vultr, AWS и т.д.)
*   **Локация:** **TOKYO (Япония)** 🇯🇵 (Критично для Bybit!)
*   **OS:** **Ubuntu 22.04** (LTS)
*   **CPU:** Желательно Dedicated (Выделенные ядра), но для тестов пойдет Shared.
*   **IP:** Избегай подсетей `38.54.x.x` (часто в бане). Если дали IP из этого диапазона — лучше сразу пересоздать.

---

## 2. Подключение (С Компьютера)

Открой **PowerShell** (на своем Windows) и введи:

```powershell
# Если пересоздал сервер на том же IP, сначала очисти старый ключ:
ssh-keygen -R <IP_АДРЕС>

# Подключение:
ssh root@<IP_АДРЕС>
```
*Введи пароль (курсор не будет двигаться, это нормально).*

---

## 3. Настройка Сервера (Внутри черного окна SSH)

Скопируй этот блок целиком и вставь в терминал сервера (ПКМ или Ctrl+V):

```bash
# 1. Обновляем систему и ставим инструменты
apt update && apt install -y build-essential curl git pkg-config libssl-dev nano

# 2. Устанавливаем Rust (компилятор)
# Если спросит выбор 1-3, просто нажми Enter (по умолчанию 1)
curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y

# 3. Применяем настройки Rust
source "$HOME/.cargo/env"

# 4. Создаем папку для бота
mkdir -p ~/hft_bot

# 5. Проверка (должен показать версию, например 1.84.0)
rustc --version
```

---

## 4. Загрузка Кода (С ТВОЕГО Компьютера)

**ВАЖНО:** Не вводи эту команду на сервере!
Открой **НОВОЕ, СИНЕЕ окно PowerShell** на своем компьютере.
Перейди в папку с проектом:

```powershell
cd D:\HFT_Rust
```

Отправь файлы "десантом" (Код + Конфиги + Секреты):

```powershell
scp -r src Cargo.toml .env root@<IP_АДРЕС>:~/hft_bot/
```
*(Замени `<IP_АДРЕС>` на реальные цифры, например `149.104.78.218`)*

---

## 5. Запуск (Внутри черного окна SSH)

Возвращаемся на сервер:

```bash
cd ~/hft_bot
cargo run --release
```

---

## 🚑 Решение Проблем

### Ошибка: `Trade Error Code: 10009, Msg: IP has been banned`
*   **Причина:** Тебе достался "грязный" IP, который Bybit уже забанил.
*   **Решение:** Удали сервер, создай новый. Попробуй попасть в другую подсеть (другие первые две цифры IP).

### Ошибка: `os error 115` (Operation now in progress)
*   **Причина:** Особенность Linux сокетов.
*   **Решение:** Уже исправлено в коде (`ws_client.rs`). Просто убедись, что залил свежую версию.

### Ошибка: `scp: No such file or directory` (при запуске scp)
*   **Причина:** Ты пытаешься запустить `scp` внутри сервера.
*   **Решение:** `scp` запускается ТОЛЬКО с твоего компьютера (Windows), чтобы отправить файлы ТУДА.

### Как редактировать файлы прямо на сервере?
Если нужно срочно поправить конфиг:
```bash
nano .env
# Ctrl+O (Сохранить), Enter, Ctrl+X (Выйти)
```
